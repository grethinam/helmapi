apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmapi.fullname" . }}
  labels:
    app: {{ include "helmapi.name" . }}
    chart: {{ include "helmapi.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "helmapi.name" . }}
    release: {{ .Release.Name }}
